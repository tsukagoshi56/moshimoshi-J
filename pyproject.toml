[project]
name = "llm-jp-moshi"
version = "0.1.0"
description = "LLM-jp-Moshi-v1 Japanese full-duplex spoken dialogue model runner"
requires-python = ">=3.10,<3.15"
dependencies = [
    "moshi<=0.2.2",
]

[tool.uv]
package = false
